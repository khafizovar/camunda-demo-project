#spring.datasource.url: jdbc:h2:file:./camunda-h2-database

camunda.bpm:
#  admin-user:
#    id: admin
#    password: admin
  deployment-resource-pattern:
    - classpath*:**/*.bpmn
    - classpath*:**/*.bpmn20.xml
    - classpath*:**/*.dmn
    - classpath*:**/*.dmn11.xml
    - classpath*:**/*.form
  database:
      type: postgres
server:
  port: '8083'
spring:
  main:
    allow-bean-definition-overriding: true
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    driverClassName: org.postgresql.Driver
    #DB connection:
    url: jdbc:postgresql://10.90.138.99:5432/cam_develop
    username: cam_admin
    password: cam_admin_password
camunda:
  bpm:
    authorization:
      enabled: true
      tenant-check-enabled: false
  run:
    auth:
      enabled: true
#  generic-properties:
#    properties:
#      default-user-permission-name-for-task: TASK_WORK

gtm:
  user: admin
  pwd: tn$iu#cnt%demo22@
  auth-url: http://gtm-back:8080
  auth:
    url: "http://gtm-back:8080/api/auth/signin"
  roles:
    url: "http://gtm-back:8080/api/v1/roles/"
  users:
    url: "http://gtm-back:8080/api/v1/ldap/users"